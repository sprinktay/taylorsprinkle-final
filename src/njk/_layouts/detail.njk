{% extends '_layouts/base.njk' %}

{% block content %}
<article class="container">
  <div class="row">
    <div class="col-sm-4 left-rail">
        <img class="img-fluid" src="img/{{ member.image }}" alt="">
    </div>
    <div class="col-sm-8">
        <h1>{{member.name}}</h1>
    </div>
  </div>
</article>
{% endblock %}
